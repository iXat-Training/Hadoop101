Use the provided data set and do the following

1. Join Posts, Comments and Users to output the data in the following fashion from a job
    <postTitle>, commented by <username1>,<username2>......

2. Also, display scores to the above, a comment could have a score, if a comment score falls between 0-3 mark it as low score, if 4-6 mark it medium, else mark it high, so the output should be as the below
  <postTitle>, commented by <username1 (score low|medium|high)>,<username2 (score low|medium|high)>......

3. Gather Tomcat logs fromm your project and show the total pages served from a given webserver

